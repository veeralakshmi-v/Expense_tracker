expense_tracker/
│
├── main.py              👉 Starts the app
├── database.py          👉 Connects to database
├── models.py            👉 Table structure (Expense)
├── schemas.py           👉 Defines how data looks (input/output)
└── crud.py              👉 All database operations (add, get, find highest)

1.main.py starts first
Inside main.py, this line runs:
    Base.metadata.create_all(bind=engine)
This tells Python to create database tables if they don’t already exist.

2.Then it connects routes:
    app.include_router(expense.router)
This loads all your API endpoints from routes/expense.py.

Flow is:
main.py → database.py (connect DB)
       → models.py (create table)
       → routes/expense.py (API endpoints)
       → crud.py (DB operations)

3. SQLALCHEMY_DATABASE_URL = "sqlite:///./expenses.db"
You do not have to create it manually.
When you run your app for the first time, FastAPI + SQLAlchemy will automatically generate this file and the table inside it.

Final:
You run: uvicorn main:app --reload
         ↓
FastAPI reads main.py
         ↓
main.py → connects to database.py
         ↓
database.py → creates SQLite connection (expenses.db)
         ↓
models.py → defines Expense table
         ↓
Base.metadata.create_all() → creates table in database
         ↓
routes/expense.py → defines URL endpoints (API)
         ↓
crud.py → performs add/get/highest functions on DB
